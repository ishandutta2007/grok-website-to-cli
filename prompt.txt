I want you to build a CLI tool that converts Grok website(ie https://grok.com) to CLI. It will be a python based package which can be deplyed to pypi. I want to use RPA(Robotic Process Automation) technologies rather than reverse proxy API calls(as it can be blocked by cloudflare but RPA can't). It should accept two arguments from user 1)prompt filepath(mandatory), 2)output file path(optional). Like any RPA, First, launch Edge browser (if it is not already launched), then open the Grok tab, or if the Grok tab is already open then just switch to the grok tab. And after that, you have to paste the prompt in the prompt box. And once the prompt is pasted and entered, you would have to wait for a minute or two for the results to appear. And once the result appears, you have to copy to clipboard the last code block part from the results. So once you have copied that code block to clipboard, since this is a CLI, either you have to output the results to the terminal itself, or if the user has passed output file name (ie the second argument) then paste(ie append) the clipboard contents into that output file.

I have issue with this selenium approach. selenium won't launch grok.com with my logged in state. secondly selenium also has good chance of getting blocked by cloudflare. So instead I want you to abandon selenium approach and use the currently opened instance of edge browser. I would suggest you can pair this CLI with a browser extension for locating the currently opened grok tab and switching to it. For locating prompt box you may have to use visual/OCR based techniches or access DOM via that paired browser extension.

add another argument: "browser", which enables user to choose Chrome browser as well. However, the default browser should remain as Edge.
